# Bennett.Extensions.Logging.FlightRecorder
Bennett.Extensions.Logging.FlightRecorder contains a FlightRecorderLogger that logs to a file.  If no errors were recorded during the program's execution, the log file is deleted.
